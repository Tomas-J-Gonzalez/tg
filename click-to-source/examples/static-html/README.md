# Static HTML Example

1. From the `click-to-source` directory, run:
   ```
   node src/bridge.js
   ```
2. Serve this folder (e.g. `npx serve .` or `python -m http.server 8080`)
3. Open http://localhost:8080
4. Ctrl+Alt+Click the paragraph or footer to open the source
